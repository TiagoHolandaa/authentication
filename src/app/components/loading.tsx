import * as React from 'react';

const Loading = () => {
  return (
    <div>
      <p>Carregando...</p>
      {/* Inclua um spinner ou animação de carregamento aqui, se desejar. */}
    </div>
  );
};

export default Loading;